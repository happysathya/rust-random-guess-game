A random guess program written in rust language.

### Build
`cargo clean && cargo build`

### Release Build
`cargo clean && cargo build --release`

### Run
`cargo run`

### Run unit tests
`cargo test`

### Run unit tests with stacktrace display
`RUST_BACKTRACE=1 cargo test`
